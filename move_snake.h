#ifndef MOVE_SNAKE_H
#define MOVE_SNAKE_H

#include <deque>

#include "snake_list.h"

//NODE* move_snake(NODE* headPtr, SnakeDirection direction);
//SDL_Rect calculate_position(NODE* curRectPos, SnakeDirection direction);
SDL_Rect check_screen_collision(SDL_Rect r);
//bool check_opposite_turn(NODE* curPosRect, SnakeDirection direction);




#endif